# kasir-pos

POS offline-first (Tauri + React) untuk UMKM Indonesia. Lihat `POS-PRD.md` dan `POS-FSD-structure.md` untuk product & architecture spec.

## Agent skills

### Issue tracker

Issues tracked as GitHub issues in `Rimba-maker/kasir-pos` (via `gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
