node_modules/
dist/
target/
.DS_Store
*.log
*.tsbuildinfo

# Tauri generated files
src-tauri/target/
src-tauri/gen/
# local SQLite data
*.db

# Internal build/process docs — kept local, not published.
# Tool-discovered files must stay in the root, so anchor them with a leading slash;
# everything else lives in one ignored folder (add new internal docs there, auto-hidden).
/CLAUDE.md
/AGENTS.md
docs/internal/
